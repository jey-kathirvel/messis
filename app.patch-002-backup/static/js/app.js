document.addEventListener("DOMContentLoaded",()=>{const form=document.querySelector("#login-form");if(!form)return;const pins=[...document.querySelectorAll("#pin input")],error=document.querySelector("#error");pins.forEach((input,i)=>{input.addEventListener("input",()=>{input.value=input.value.replace(/\D/g,"").slice(0,1);if(input.value&&i<5)pins[i+1].focus()});input.addEventListener("keydown",e=>{if(e.key==="Backspace"&&!input.value&&i>0)pins[i-1].focus()});input.addEventListener("paste",e=>{e.preventDefault();const value=e.clipboardData.getData("text").replace(/\D/g,"").slice(0,6);value.split("").forEach((n,x)=>pins[x].value=n);pins[Math.min(value.length,5)].focus()})});document.querySelector("#clear").onclick=()=>{pins.forEach(x=>x.value="");pins[0].focus();error.classList.remove("show")};form.addEventListener("submit",e=>{e.preventDefault();const user=document.querySelector("#user-id").value.trim(),pass=pins.map(x=>x.value).join("");if(!user||!/^\d{6}$/.test(pass)){error.textContent="Enter a valid User ID and complete 6-digit passcode.";error.classList.add("show");return}location.href="/dashboard"})});
